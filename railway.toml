[build]
builder = "DOCKERFILE"

[deploy]
healthcheckPath = "/api/v2/monitor/health"
healthcheckTimeout = 1800
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
requiredMountPath = "/opt/airflow/data"
